build:
    dockerfile: ./Dockerfile.build

database:
    image: postgres
    service: true
    volumes:
       - "pgdata:/var/lib/postgresql/data"
    ports: 5432

test:
    dockerfile: ./Dockerfile.test
    depends:
      - build
      - database
    command: runtests
